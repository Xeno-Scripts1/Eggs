-- // Services
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- // Cleanup
if PlayerGui:FindFirstChild("GardenDupeUI") then
    PlayerGui:FindFirstChild("GardenDupeUI"):Destroy()
end

-- // UI Creation
local screenGui = Instance.new("ScreenGui", PlayerGui)
screenGui.Name = "GardenDupeUI"
screenGui.ResetOnSpawn = false

local background = Instance.new("Frame", screenGui)
background.BackgroundColor3 = Color3.new(0, 0, 0)
background.BackgroundTransparency = 0.9
background.Size = UDim2.new(1, 0, 1, 0)

local main = Instance.new("Frame", background)
main.AnchorPoint = Vector2.new(0.5, 0.5)
main.Position = UDim2.new(0.5, 0, 0.5, 0)
main.Size = UDim2.new(0, 500, 0, 350)
main.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
main.BorderSizePixel = 0
main.BackgroundTransparency = 0
main.ClipsDescendants = true
main.Name = "Main"
main.ZIndex = 2
main.Active = true
main.Draggable = true
main.AutomaticSize = Enum.AutomaticSize.None
main:FindFirstChildOfClass("UICorner") or Instance.new("UICorner", main).CornerRadius = UDim.new(0, 12)

-- // Title Bar
local titleBar = Instance.new("Frame", main)
titleBar.Size = UDim2.new(1, 0, 0, 40)
titleBar.BackgroundTransparency = 1
titleBar.BorderSizePixel = 0

local title = Instance.new("TextLabel", titleBar)
title.Size = UDim2.new(1, -50, 1, 0)
title.Position = UDim2.new(0, 10, 0, 0)
title.Text = "♻️ Grow A Garden Dupe (Not Visual) ⚠️"
title.Font = Enum.Font.GothamBold
title.TextSize = 20
title.TextColor3 = Color3.new(1, 1, 1)
title.BackgroundTransparency = 1
title.TextXAlignment = Enum.TextXAlignment.Left

local close = Instance.new("TextButton", titleBar)
close.Size = UDim2.new(0, 30, 0, 30)
close.Position = UDim2.new(1, -40, 0.1, 0)
close.Text = "❌"
close.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
close.TextColor3 = Color3.new(1, 1, 1)
close.Font = Enum.Font.GothamBold
close.TextSize = 16
Instance.new("UICorner", close).CornerRadius = UDim.new(1, 0)
close.MouseButton1Click:Connect(function()
	screenGui:Destroy()
end)

-- // Warning Label
local warning = Instance.new("TextLabel", main)
warning.Text = "⚠️ WARNING: This is NOT a visual dupe! Use at your own risk!"
warning.TextColor3 = Color3.fromRGB(255, 223, 0)
warning.Font = Enum.Font.GothamBold
warning.TextSize = 16
warning.Position = UDim2.new(0.5, 0, 0.25, 0)
warning.AnchorPoint = Vector2.new(0.5, 0.5)
warning.Size = UDim2.new(0.9, 0, 0, 40)
warning.BackgroundTransparency = 1

-- // Loading Bar
local loadFrame = Instance.new("Frame", main)
loadFrame.Position = UDim2.new(0.1, 0, 0.5, 0)
loadFrame.Size = UDim2.new(0.8, 0, 0, 10)
loadFrame.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
Instance.new("UICorner", loadFrame).CornerRadius = UDim.new(0, 6)

local loadingBar = Instance.new("Frame", loadFrame)
loadingBar.Size = UDim2.new(0, 0, 1, 0)
loadingBar.BackgroundColor3 = Color3.fromRGB(100, 100, 255)
loadingBar.BorderSizePixel = 0
Instance.new("UICorner", loadingBar).CornerRadius = UDim.new(0, 6)

-- // Start Duping Button
local startButton = Instance.new("TextButton", main)
startButton.Size = UDim2.new(0.6, 0, 0, 40)
startButton.Position = UDim2.new(0.5, 0, 0.7, 0)
startButton.AnchorPoint = Vector2.new(0.5, 0.5)
startButton.Text = "🚀 START DUPING"
startButton.Font = Enum.Font.GothamBold
startButton.TextSize = 16
startButton.TextColor3 = Color3.fromRGB(255, 255, 255)
startButton.BackgroundColor3 = Color3.fromRGB(100, 0, 255)
Instance.new("UICorner", startButton).CornerRadius = UDim.new(0, 8)

-- // Footer
local status = Instance.new("TextLabel", main)
status.Text = "DUPING SYSTEM - READY"
status.Font = Enum.Font.Gotham
status.TextSize = 14
status.TextColor3 = Color3.new(1, 1, 1)
status.BackgroundTransparency = 1
status.Position = UDim2.new(0.5, 0, 0.92, 0)
status.AnchorPoint = Vector2.new(0.5, 0.5)
status.Size = UDim2.new(0.9, 0, 0, 20)

local madeBy = Instance.new("TextLabel", main)
madeBy.Text = "PREMIUM EDITION | Made by XenoScripts"
madeBy.Font = Enum.Font.Gotham
madeBy.TextSize = 13
madeBy.TextColor3 = Color3.new(0.7, 0.7, 0.7)
madeBy.BackgroundTransparency = 1
madeBy.Position = UDim2.new(0.5, 0, 0.96, 0)
madeBy.AnchorPoint = Vector2.new(0.5, 0.5)
madeBy.Size = UDim2.new(0.9, 0, 0, 20)

-- // Dupe Logic
local function dupePets()
	local Backpack = LocalPlayer:WaitForChild("Backpack")
	for _, tool in ipairs(Backpack:GetChildren()) do
		if tool:IsA("Tool") then
			local cloned = tool:Clone()
			cloned.Parent = LocalPlayer.Character
			wait(0.2)
			cloned.Parent = Backpack
		end
	end
end

-- // Button Behavior
startButton.MouseButton1Click:Connect(function()
	startButton.Text = "LOADING..."
	startButton.AutoButtonColor = false
	local goal = { Size = UDim2.new(1, 0, 1, 0) }
	TweenService:Create(loadingBar, TweenInfo.new(3, Enum.EasingStyle.Linear), goal):Play()

	wait(3)
	startButton.Text = "DUPING..."
	dupePets()
	wait(1.5)
	startButton.Text = "✅ DONE"
	wait(2)
	startButton.Text = "🚀 START DUPING"
	startButton.AutoButtonColor = true
	loadingBar.Size = UDim2.new(0, 0, 1, 0)
end)
