local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- Remove existing UI if present
if PlayerGui:FindFirstChild("GrowGardenDupeUI") then
    PlayerGui.GrowGardenDupeUI:Destroy()
end

-- Create ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "GrowGardenDupeUI"
ScreenGui.Parent = PlayerGui
ScreenGui.ResetOnSpawn = false

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 380, 0, 230)
MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame.BackgroundColor3 = Color3.fromRGB(70, 30, 30) -- dark muted reddish-brown
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 18)
UICorner.Parent = MainFrame

-- Close Button (X) top right
local CloseButton = Instance.new("TextButton")
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Position = UDim2.new(1, -35, 0, 10) -- 10px from top-right corner
CloseButton.AnchorPoint = Vector2.new(0, 0)
CloseButton.BackgroundColor3 = Color3.fromRGB(90, 30, 30)
CloseButton.BorderSizePixel = 0
CloseButton.Text = "✕"
CloseButton.TextColor3 = Color3.fromRGB(255, 180, 180)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextScaled = true
CloseButton.AutoButtonColor = true
CloseButton.ZIndex = 15
CloseButton.Parent = MainFrame

local CloseButtonCorner = Instance.new("UICorner")
CloseButtonCorner.CornerRadius = UDim.new(0, 8)
CloseButtonCorner.Parent = CloseButton

-- Hover effects for Close button
CloseButton.MouseEnter:Connect(function()
    TweenService:Create(CloseButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(150, 50, 50), TextColor3 = Color3.fromRGB(255, 100, 100)}):Play()
end)
CloseButton.MouseLeave:Connect(function()
    TweenService:Create(CloseButton, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(90, 30, 30), TextColor3 = Color3.fromRGB(255, 180, 180)}):Play()
end)

CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- Glow effect frame behind main frame for glowing button edges (for the button)
local GlowFrame = Instance.new("Frame")
GlowFrame.Size = UDim2.new(0.88, 0, 0, 70)
GlowFrame.Position = UDim2.new(0.06, 0, 0.65, 0)
GlowFrame.BackgroundColor3 = Color3.fromRGB(140, 43, 226) -- purple glow
GlowFrame.BorderSizePixel = 0
GlowFrame.ZIndex = 9
GlowFrame.ClipsDescendants = false
GlowFrame.AnchorPoint = Vector2.new(0, 0)
GlowFrame.Parent = MainFrame

local GlowUICorner = Instance.new("UICorner", GlowFrame)
GlowUICorner.CornerRadius = UDim.new(0, 20)

local GlowGradient = Instance.new("UIGradient", GlowFrame)
GlowGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(180, 90, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(60, 50, 220))
}
GlowGradient.Rotation = 45

-- Title with warning icon (⚠️)
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -40, 0, 50)
Title.Position = UDim2.new(0, 20, 0, 15)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.GothamBold
Title.Text = "⚠️ Grow A Garden Dupe (Not Visual)"
Title.TextColor3 = Color3.fromRGB(255, 200, 180)
Title.TextScaled = true
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = MainFrame

-- Warning Label (smaller, lighter font)
local WarningLabel = Instance.new("TextLabel")
WarningLabel.Size = UDim2.new(1, -40, 0, 45)
WarningLabel.Position = UDim2.new(0, 20, 0, 70)
WarningLabel.BackgroundTransparency = 1
WarningLabel.Font = Enum.Font.GothamSemibold
WarningLabel.Text = "WARNING: This is NOT a visual dupe! Use at your own risk!"
WarningLabel.TextColor3 = Color3.fromRGB(230, 160, 140)
WarningLabel.TextScaled = true
WarningLabel.TextWrapped = true
WarningLabel.TextXAlignment = Enum.TextXAlignment.Left
WarningLabel.Parent = MainFrame

-- Start Button positioned exactly over GlowFrame, with higher ZIndex so it’s fully clickable and visible
local StartButton = Instance.new("TextButton")
StartButton.Size = UDim2.new(0.88, 0, 0, 60)
StartButton.Position = UDim2.new(0.06, 0, 0.65, 5)
StartButton.AnchorPoint = Vector2.new(0, 0)
StartButton.BackgroundColor3 = Color3.fromRGB(65, 50, 220)
StartButton.BorderSizePixel = 0
StartButton.Text = "START DUPING"
StartButton.TextColor3 = Color3.fromRGB(255, 255, 255)
StartButton.Font = Enum.Font.GothamBold
StartButton.TextScaled = true
StartButton.ZIndex = 10
StartButton.Parent = MainFrame

local StartButtonCorner = Instance.new("UICorner")
StartButtonCorner.CornerRadius = UDim.new(0, 20)
StartButtonCorner.Parent = StartButton

-- Button hover effect (glow)
StartButton.MouseEnter:Connect(function()
    TweenService:Create(GlowFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {BackgroundColor3 = Color3.fromRGB(180, 90, 255)}):Play()
    TweenService:Create(StartButton, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {BackgroundColor3 = Color3.fromRGB(100, 70, 255)}):Play()
end)
StartButton.MouseLeave:Connect(function()
    TweenService:Create(GlowFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {BackgroundColor3 = Color3.fromRGB(140, 43, 226)}):Play()
    TweenService:Create(StartButton, TweenInfo.new(0.3, Enum.EasingStyle.Quad), {BackgroundColor3 = Color3.fromRGB(65, 50, 220)}):Play()
end)

-- Loading Bar Background (hidden initially)
local LoadingBarBackground = Instance.new("Frame")
LoadingBarBackground.Size = UDim2.new(0.85, 0, 0, 28)
LoadingBarBackground.Position = UDim2.new(0.5, 0, 0.55, 0)
LoadingBarBackground.AnchorPoint = Vector2.new(0.5, 0)
LoadingBarBackground.BackgroundColor3 = Color3.fromRGB(45, 30, 30)
LoadingBarBackground.BorderSizePixel = 0
LoadingBarBackground.ClipsDescendants = true
LoadingBarBackground.Visible = false
LoadingBarBackground.ZIndex = 11
LoadingBarBackground.Parent = MainFrame

local LoadingBarCorner = Instance.new("UICorner")
LoadingBarCorner.CornerRadius = UDim.new(0, 14)
LoadingBarCorner.Parent = LoadingBarBackground

local LoadingBarFill = Instance.new("Frame")
LoadingBarFill.Size = UDim2.new(0, 0, 1, 0)
LoadingBarFill.BackgroundColor3 = Color3.fromRGB(140, 50, 220)
LoadingBarFill.BorderSizePixel = 0
LoadingBarFill.ZIndex = 12
LoadingBarFill.Parent = LoadingBarBackground

local LoadingFillCorner = Instance.new("UICorner")
LoadingFillCorner.CornerRadius = UDim.new(0, 14)
LoadingFillCorner.Parent = LoadingBarFill

local LoadingPercent = Instance.new("TextLabel")
LoadingPercent.Size = UDim2.new(1, 0, 1, 0)
LoadingPercent.BackgroundTransparency = 1
LoadingPercent.Font = Enum.Font.GothamBold
LoadingPercent.TextColor3 = Color3.fromRGB(255, 255, 255)
LoadingPercent.TextScaled = true
LoadingPercent.Text = "0%"
LoadingPercent.ZIndex = 13
LoadingPercent.Parent = LoadingBarBackground

-- Status Text below button
local StatusText = Instance.new("TextLabel")
StatusText.Size = UDim2.new(1, -40, 0, 30)
StatusText.Position = UDim2.new(0, 20, 0, 140)
StatusText.BackgroundTransparency = 1
StatusText.Font = Enum.Font.GothamSemibold
StatusText.Text = "DUPING SYSTEM - READY"
StatusText.TextColor3 = Color3.fromRGB(200, 200, 200)
StatusText.TextScaled = true
StatusText.TextXAlignment = Enum.TextXAlignment.Center
StatusText.Parent = MainFrame

-- Edition Text below StatusText
local EditionText = Instance.new("TextLabel")
EditionText.Size = UDim2.new(1, -40, 0, 30)
EditionText.Position = UDim2.new(0, 20, 0, 175)
EditionText.BackgroundTransparency = 1
EditionText.Font = Enum.Font.GothamSemibold
EditionText.Text = "PREMIUM EDITION"
EditionText.TextColor3 = Color3.fromRGB(180, 180, 180)
EditionText.TextScaled = true
EditionText.TextXAlignment = Enum.TextXAlignment.Center
EditionText.Parent = MainFrame

-- Dragging logic
local dragging = false
local dragInput, dragStart, startPos

local function update(input)
    local delta = input.Position - dragStart
    MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X,
                                   startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)

-- Dupe pet function
local function dupePet()
    local equippedTool = Character:FindFirstChildOfClass("Tool")
    if not equippedTool then
        StartButton.Text = "⚠️ No pet equipped!"
        wait(1.5)
        StartButton.Text = "START DUPING"
        return
    end

    for _, child in pairs(Character:GetChildren()) do
        if child.Name:find("_VisualClone") then
            child:Destroy()
        end
    end

    local clone = equippedTool:Clone()
    clone.Name = equippedTool.Name .. "_VisualClone"

    local handle = clone:FindFirstChild("Handle") or clone:FindFirstChildWhichIsA("BasePart")
    if handle then
        handle.Anchored = false
        handle.CanCollide = false
        clone.Parent = Character

        local hand = Character:FindFirstChild("RightHand") or Character:FindFirstChild("Right Arm")
        if hand then
            local weld = Instance.new("WeldConstraint")
            weld.Part0 = hand
            weld.Part1 = handle
            weld.Parent = handle

            handle.CFrame = hand.CFrame * CFrame.new(0, -0.5, -1)
        end
    end

    StartButton.Text = "✅ Pet Duplicated!"
    wait(1.5)
    StartButton.Text = "START DUPING"
end

-- Animate loading bar
local function animateLoadingBar(duration)
    LoadingBarFill.Size = UDim2.new(0, 0, 1, 0)
    LoadingBarBackground.Visible = true
    LoadingPercent.Text = "0%"
    StartButton.AutoButtonColor = false
    StartButton.Active = false
    StartButton.Text = "⏳ Duplicating..."
    StatusText.Text = "DUPING SYSTEM - RUNNING"

    local startTime = tick()
    local connection

    connection = RunService.RenderStepped:Connect(function()
        local elapsed = tick() - startTime
        local progress = math.clamp(elapsed / duration, 0, 1)
        LoadingBarFill.Size = UDim2.new(progress, 0, 1, 0)
        LoadingPercent.Text = string.format("%d%%", math.floor(progress * 100))
        if progress >= 1 then
            connection:Disconnect()
        end
    end)

    wait(duration)

    LoadingBarBackground.Visible = false
    StartButton.AutoButtonColor = true
    StartButton.Active = true
    StartButton.Text = "START DUPING"
    StatusText.Text = "DUPING SYSTEM - READY"
end

-- Button click
StartButton.MouseButton1Click:Connect(function()
    if not StartButton.Active then return end
    coroutine.wrap(function()
        animateLoadingBar(7)
        dupePet()
    end)()
end)
